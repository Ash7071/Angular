
  // im creating interface here //////////////////////////////////
  // if u want u need to create class also but the class is too hard //////////////////////////////////
  // here im using simple javascript///////

export interface Users{
     "id": string;
     "name": string;
     "company": string;
     "agent": number;
}
